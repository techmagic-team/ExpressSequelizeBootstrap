'use strict';

var expect = require('expect.js');

describe('model/task', function () {
    beforeEach(function () {
        // this.User = require('../../model').User;
    });

    //describe('create', function () {
    //    it('creates a task', function () {
    //        var email = new Date().getTime().toString() + 'johndoe@test.com';
    //        return this.User.create({email: email}).bind(this).then(function (user) {
    //            expect(user.email).to.equal(email);
    //        });
    //    });
    //});
});
